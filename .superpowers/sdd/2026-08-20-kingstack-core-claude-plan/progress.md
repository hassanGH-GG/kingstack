# SDD ledger — plan: docs/superpowers/plans/2026-08-20-kingstack-core-claude-plan.md

Foundation dependency: independently approved canonical commit `639fc04b356c0a18b3a689dec465d1dbc914ea73`.

## Global rulings

- Keep `~/.claude` and `~/.codex` read-only through all six tasks.
- Render only as immutable in-memory bundles; no mutable `.staging/` publication and no adapter activation/current link is authorized.
- Pstack remains upstream-owned; generated pstack skills are never hand-edited.
- Every task gets a fresh implementer and independent review before its consumer starts.
- The mandatory Hassan cutover stop remains after all staged Claude/Codex parity, immediately before the first live manifest-owned link.

## Task log

- Task 1 dispatch base: `639fc04`; define the exact seven-field adapter contract, capability catalog/matrices, and synthetic third-adapter extension proof.
- Task 1 complete through `fa7a2fc`; independent final review APPROVED with no findings. Focused 27/27, full 58/58, exact 13-capability matrices, honest Codex unsupported states, safe cross-platform/NFC ownership, schema/runtime agreement, native state unchanged.
- Task 2 dispatch base: `fa7a2fc`; freeze root `CLAUDE.md`, split only at existing heading boundaries, and require byte-identical Claude rendering into `.staging/claude` with no native write.
- Task 2 implementation `f363592` proved exact guidance parity. Fix 1 `5ca29bb` hardened mutable staging but independent review found further namespace/cleanup races. Hassan correctly identified an unending fix cycle. Ruling: stop patching mutable staging and eliminate that interface. Renderer becomes pure `relative path -> bytes`; only a later immutable release builder may materialize a bundle. Cost if wrong: downstream plans must consume bundle manifests/bytes rather than `.staging` paths, but the entire staging race class disappears.
- Pure-bundle cross-plan rewrite independently APPROVED and committed at `bf906ff`. Replacement Task 2 base is `bf906ff`; it must remove the existing mutable writer/CLI/tests while preserving the already-proven fragment bytes and descriptor-confined source reads.
- Replacement Task 2 complete through `8ad1306`; independent final review APPROVED with no findings. Focused contract+render 42/42, full 73/73, exact 9,525-byte Claude parity, pure provider dispatch proven by a synthetic third adapter, shared portable path validation, no mutable staging API/reference, native state unchanged.
- Task 3 dispatch base: `8ad1306`; replace shared vendor model names with portable work classes, model tiers, effort, adjacent fallback, and private availability overrides while preserving exact Claude behavior and keeping Codex guidance free of Claude model names.
- Task 3 complete through `9d5e48b`; independent final review APPROVED with no findings. Focused routing+contract+render 53/53, full 84/84, all 49 malformed-key boundary combinations return stable `RoutingError`, waiting overrides validate before no-model return, foreign-name scans clean, native state unchanged.
- Task 4 dispatch base: `9d5e48b`; build a single-source skill catalog preserving all 65 baseline Claude names, separate authored/pstack/adopted/plugin-manager ownership, and refactor pstack sync to emit pure adapter bundle entries without mutating native skill trees.
- Task 4 implementation `190587c` plus grouped safety fix `07cd989` is locally committed, clean, and unpushed. Last green evidence before closure review: focused 18/18, full 102/102; Claude accounts 53 bundled + 12 plugin-managed; Codex accounts 41 bundled + 11 plugin-managed + 13 unsupported; pstack remains `63d938c`; native protected hashes/types/current-link absences unchanged.
- Task 4 closure review confirmed source/clobber/mixed-ownership/Codex-direct-token/future-adapter basics, but five Important gaps remain open at clean HEAD `07cd989`: (1) close root/upstream descriptors from the first acquisition across every early failure; (2) structurally constrain model/tool/path/host transform token shapes so a whole instruction paragraph cannot masquerade as an allowed token, and keep parity independent; (3) populate dependency edges and propagate Codex unsupported status through architect→how/why/arena/interrogate, blast-radius→how/why/arena, teach→how/why, figure-it-out→poteto-mode/show-me-your-work/architect, with optional principle-prove-it-works→show-me-your-work accounted explicitly; (4) accept only baseline frontmatter forms—52 exact stable names plus explicit legacy `Poteto Mode`, and nonempty quoted/plain/folded-string descriptions—while rejecting empty/comment/null/boolean/collection/all-caps aliases; (5) descriptor-confine adapter discovery and reject symlinked/swapped adapter declarations. No closure test or production edit for these five has landed yet.
- Quota ruling at 14% remaining: finish only Task 4 closure, then stop. Do not start Tasks 5–6 in this session. Future cadence: one independent closure review for lifecycle+Claude parity, one for shared memory, one for the complete Codex adapter, mandatory pre-link activation/rollback review, and one final end-to-end review. Use balanced/medium for ordinary implementation; reserve frontier/high for filesystem/data-loss/cutover and final review.
- Task 4 closure implementation complete at `c53ae8752a0c8b3ca533b94cb38d2792bacb5b44`; the five grouped closure REDs are fixed. Independent closure review of `07cd989..c53ae87` APPROVED with no findings on 2026-08-21.
- Task 5–6 / Phase A dispatch base: `8541cfd`; portable lifecycle handlers, Claude normalizer, in-memory hook bundle files, bundle syntax checks, and rendered Claude capability-ID parity. Cursor Agent adapter recorded as deferred Phase H; not implemented in this phase. Native homes remain read-only.
