# Legacy Claude checkout

`~/.claude` remains a real git checkout and a live Claude home. It is not the
canonical kingstack source. Do not delete it. Do not replace it with a symlink.
Canonical work happens in `~/Desktop/Work/kingstack`.
