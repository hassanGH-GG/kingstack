# The old Claude checkout

`~/.claude` is still a git checkout and still the live Claude home. It is
not the kingstack source anymore. Do not delete it. Do not turn it into
a symlink. Edit the kingstack checkout instead. Hassan's default is
`~/Desktop/Work/kingstack`. A teammate clones anywhere and runs
`docs/SETUP.md`. Set `KINGSTACK_ROOT` if hooks cannot see the clone.
